{{#if cafes.length}}
<nav class="panel">
  {{#each cafes}}
  <div class="panel-block">
    <span class="panel-icon"><i class="fas fa-mug-hot" aria-hidden="true"></i></span>
    {{name}} — {{category}}
  </div>
  {{/each}}
</nav>
{{else}}
<p class="mb-4">No cafes yet.</p>
{{/if}}
